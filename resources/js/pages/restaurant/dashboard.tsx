import RoleDashboard from '@/components/role-dashboard';

export default function RestaurantDashboard() {
    return (
        <RoleDashboard
            role="Restaurant"
            description="Manage your menu, incoming orders, and restaurant operations."
        />
    );
}
