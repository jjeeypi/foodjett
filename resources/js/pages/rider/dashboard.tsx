import RoleDashboard from '@/components/role-dashboard';

export default function RiderDashboard() {
    return (
        <RoleDashboard
            role="Rider"
            description="Find delivery offers and track active deliveries and earnings."
        />
    );
}
